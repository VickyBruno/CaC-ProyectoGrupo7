// Creamos nuestra aplicación para el Footer.
const miApp0 = Vue.createApp({
    components: {
        'footer-walter': componente0,
    },
    data() {
        return {
        }
    },
}).mount("#app-footer");

// Creamos nuestra aplicación para las FAQ.
const miApp1 = Vue.createApp({
    components: {
        'faqs-walter': componente1,
    },
    data() {
        return {
        }
    },
}).mount("#app-faq")


